<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ami.mapper.ClazzMapper">

    <select id="getClazzList" resultType="org.ami.pojo.Clazz">
        select c.*,
               e.name as masterName,
               case
                   when curdate() > c.end_date then '已结课'
                   when curdate() &lt; c.begin_date then '未开班'
                   else '在读'
               end as status
        from clazz c
        left join emp e on master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name like concat('%', #{name}, '%')
            </if>
            <if test="begin != null and end != null">
                and c.end_date between #{begin} and #{end}
            </if>
        </where>
        order by update_time
    </select>
</mapper>