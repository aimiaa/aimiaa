<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ami.mapper.EmpExprMapper">


    <!--
        foreach 批量插入：
        collection: 要遍历的集合参数名
        item: 每次迭代中当前元素的变量名
        separator: 每次迭代生成的SQL片段之间的分隔符
        open: 遍历开始前拼接的内容（可选）
        close: 遍历结束后拼接的内容（可选）
        index: 当前迭代的索引，从0开始（可选）
    -->
    <insert id="addEmpExpr">
        insert into emp_expr(emp_id, begin, end, company, job) VALUES
        <foreach collection="exprList" item="expr" separator="," >
            (#{expr.empId}, #{expr.begin}, #{expr.end}, #{expr.company}, #{expr.job})
        </foreach>
    </insert>
    <delete id="deleteExprByEmpId">
        delete from emp_expr where emp_id in
        <foreach item="empId" collection="empIds" separator="," open="(" close=")">
            #{empId}
        </foreach>
    </delete>
</mapper>